services:
  edge-proxy:
    image: nginx:1.25-alpine
    container_name: learning-app-edge-proxy
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./active.conf:/etc/nginx/conf.d/default.conf:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"