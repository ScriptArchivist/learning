# Только тесты, без include!
run-tests:
  stage: test
  image: alpine:latest
  script:
    - echo "✅ Тесты для ветки $CI_COMMIT_BRANCH"
    - echo "📝 Коммит: $CI_COMMIT_SHORT_SHA"
    - ls -la
    - echo "✅ Все проверки пройдены!"
  rules:
    - if: '$CI_COMMIT_BRANCH !~ /^(main|master)$/'